import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
    <ReactLoading className='Loading' type='spin' color='white' height={'50px'} width={'50px'} />
);

export default Loading;